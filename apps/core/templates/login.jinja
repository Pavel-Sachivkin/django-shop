<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Welcome to django-shop</title>
  <link rel="icon" type="image/png" href="/static/img/favicon.png">
  <link rel="stylesheet" href="/static/css/login-form.css">
</head>

<body>
  <div class="login-box">
    <h2>Вход</h2>
    <form class="authorization" method="post" action="users/login/">
      {% csrf_token %}
      {{ form.as_p }}
      <div class="user-box">
        {{ form.email.errors }}
        {{ form.email }}
        <input type="email" name="email" required="" />
        <label>E-mail{{ form.email.label }}</label>
      </div>
      <div class="user-box">
        {{ form.password.errors }}
        <input type="password" name="password" required="" autocomplete="on" />{{ form.password }}
        <label>Password{{ form.password.label }}</label>
      </div>
      <button>
        <span></span>
        <span></span>
        <span></span>
        <span></span>
        Войти
      </button>
    </form>
  </div>
  <script src="/static/js/authorization.js"></script>
</body>

</html>